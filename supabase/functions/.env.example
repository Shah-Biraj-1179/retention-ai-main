## Optional local function env file.
## You can keep the same non-VITE values in the root `.env` and sync them with:
## supabase secrets set --env-file .env --project-ref uogfbedhjmcuwehahknh
GEMINI_API_KEY=
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
